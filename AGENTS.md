# 코딩 에이전트 작업 안내

이 저장소는 코딩 문제와 학습 정리를 기록하고 Git 브랜치·PR·동료 리뷰를 연습하는 스터디입니다.
참여자의 자연어 요청을 기존 `study.py` 명령으로 처리해 반복 입력을 줄이세요.
이 파일은 작업 지침입니다. 별도 AI 서비스나 자동 실행 기능을 설치하지 않습니다.

## 먼저 확인할 것

1. [README.md](README.md)와 요청한 기록 유형의 템플릿을 읽으세요. 세부 동작은 [docs/templates.md](docs/templates.md), Git 문제 해결은 [CONTRIBUTING.md](CONTRIBUTING.md)를 참고하세요.
2. 저장소 루트에서 `git status --short`, `git branch --show-current`를 확인하세요. 기존 변경과 사용자의 원본 파일을 보존하세요.
3. `.study/config.json`이 있으면 기존 사용자명·언어·최초 목표 설정을 사용하세요. 없으면 요청에 제공된 GitHub 사용자명으로 `init`을 실행하세요. 사용자명이 없으면 물어보고, 저장소 소유자나 Git 이메일에서 추측하지 마세요.
4. 날짜는 한국 시간(KST) 기준입니다. 사용자가 날짜를 지정하면 `start`, `new`/`note`, `done`, `prepare`에 같은 `--date YYYY-MM-DD`를 사용하세요.
5. 정보가 이미 요청이나 파일에 있으면 다시 묻지 마세요. URL·원본 파일·필수 학습 내용 등이 부족하면 가능한 초안 작업을 먼저 하고 누락된 정보만 요청하세요.

Python 3.10 이상이 필요하며 외부 패키지는 없습니다. Windows에서는 사용 가능한 `py -3` 또는 `python`을 쓰세요. 옵션은 `python3 study.py help 명령어`로 확인할 수 있습니다.
사용자가 현재 진행 단계를 잊었거나 다음 명령을 물으면 먼저 `python3 study.py status --date YYYY-MM-DD`로 확인하세요.

## 초기 설정과 목표

- 최초 설정: `python3 study.py init 사용자명 --lang python --goal 5`
- 목표 없이 기록: `python3 study.py init 사용자명 --goal none`
- 목표 생략 시 `study.json`의 기본 목표(현재 5건)가 적용됩니다. 임의로 자율 참여를 선택하지 마세요.
- 문제와 정리는 완료 1개를 각각 1건으로 합산합니다. 혼합 참여를 위한 별도 모드는 없습니다.
- 첫 `new` 또는 `note`가 `members/사용자명.json`을 만듭니다. `start --goal`을 쓰면 브랜치 생성 시에도 그 파일이 만들어질 수 있습니다.
- 그날만 목표를 바꾸려는 요청에는 `python3 study.py start --goal 3 --date YYYY-MM-DD` 또는 `start --goal none`을 사용하세요. 목표는 문제와 정리를 합쳐 완료 기록 수로 셉니다. `--goal`을 생략하면 init의 기본 목표가 적용됩니다.
- 이미 시작한 날짜의 목표를 수정하려는 요청에는 `python3 study.py goal 10 --date YYYY-MM-DD`를 사용하세요. `--date`는 해당 하루에만 적용됩니다.
- 앞으로의 기본 목표를 바꾸라는 요청에는 `python3 study.py goal 3 --from YYYY-MM-DD` 또는 `goal none --from YYYY-MM-DD`를 사용하세요. `init` 재실행으로 기존 목표 이력을 바꾸려 하지 마세요.

## 일일 브랜치 선택

- 현재 브랜치가 해당 사용자의 해당 날짜 `study/사용자명/YYYY-MM-DD`이면 계속 사용하세요. 매 기록마다 `start`를 실행하지 마세요.
- 해당 브랜치가 없고 작업 트리가 깨끗하면 `python3 study.py start --date YYYY-MM-DD`를 실행하세요. 사용자가 그날 목표를 제공하면 같은 명령에 `--goal 건수|none`을 붙입니다. 최신 원격 기준 브랜치에서 새 브랜치를 만듭니다.
- 해당 날짜의 로컬 브랜치가 이미 있으면 상태를 확인하고 그 브랜치를 사용하세요. 원격에만 있으면 해당 브랜치를 fetch하고 로컬에서 이어서 작업하세요.
- 이미 merge한 날짜의 추가 기록이나 fork 참여처럼 기본 흐름과 다르면 PR·원격 상태와 CONTRIBUTING 안내를 먼저 확인하세요.
- 다른 브랜치에 미커밋 변경이 있으면 작업을 버리거나 임의로 커밋·stash하지 마세요. 사용자에게 변경 위치와 브랜치 전환에 필요한 조치를 알려주세요.

## 코딩 문제 기록

사용자가 제공한 URL, 제목, 풀이 파일 또는 코드 본문을 사용하세요. 에이전트에게 요청할 때는 별도 파일 경로보다 코드 블록을 직접 붙여 넣는 방식이 간단합니다.

```bash
python3 study.py new 문제URL --title "문제 제목" --lang python --source "풀이파일경로" --date YYYY-MM-DD
```

- `--source`는 파일을 복사합니다. 사용자가 코드 블록을 직접 제공하면 `--source`를 사용하지 말고 생성된 기록 폴더의 `solution.*`에 그 코드를 저장하세요. 언어가 Python이 아니면 실제 언어를 지정하세요. 난이도·풀이 방식(`self`/`hint`/`answer`)·자료구조·알고리즘을 알면 `new` 또는 `done` 옵션으로 함께 기록하세요.
- 코드 블록은 사용자가 제공한 내용 그대로를 우선 보존하고, 파일명·들여쓰기·문법을 확인하세요. 사용자가 수정해 달라고 하지 않은 알고리즘을 임의로 바꾸지 마세요. 대화에 코드가 없고 파일 경로만 있으면 `--source`를 사용합니다.
- CLI가 출력한 기록 폴더를 사용하세요. 이미 존재하면 `new`를 반복하거나 덮어쓰지 말고 기존 기록을 읽어 수정하세요.
- 생성된 README의 `## 문제`, `## 풀이`, `## 확인한 예제 / 경계 조건` 제목을 유지하고 TODO 본문을 채우세요. 공용 `templates/problem.md`는 일일 기록 작업에서 수정하지 마세요.
- 문제 요약은 제공된 설명 또는 확인한 원문을 근거로, 풀이 설명은 실제 코드에 맞춰 작성하세요. URL과 제목이 다르면 확인이 필요합니다. 원문에 접근할 수 없으면 설명을 지어내지 마세요.
- 기록 정리 요청만 받았다면 사용자의 풀이 알고리즘을 임의로 교체하지 마세요. 오류를 발견하면 설명하고, 코드 수정 요청이 있으면 그 범위에서 수정하세요.
- 예제 실행 여부·채점 결과·풀이 시간·개인의 회고를 만들어내지 마세요. 예상 결과와 실제 실행 결과를 구분하세요. `--minutes`는 알려진 경우에만 넣으세요.
- `done`은 정답 채점을 하지 않습니다. 사용자가 채점 사이트에서 정답을 확인했다고 알렸고 필수 내용이 준비되면 아래를 실행하세요. 대상은 생략(작성 중 기록 1개일 때), 문제 번호, 생성된 폴더명, 문제 URL 모두 사용할 수 있습니다. 채점 확인이 없으면 README 초안과 검증까지 진행하고 완료 여부 확인을 요청하세요.

```bash
python3 study.py done programmers-문제번호 --date YYYY-MM-DD
```

- 같은 날짜의 모든 draft를 완료 처리할 때는, 사용자가 모든 문제의 정답 확인을 알린 경우에만 `python3 study.py done --all --date YYYY-MM-DD`를 사용하세요. 모든 기록을 먼저 검증하므로 하나라도 실패하면 완료 처리하지 않습니다. `--all`에는 시간·난이도 등 개별 기록 옵션을 함께 지정할 수 없습니다.

## 학습 정리 기록

```bash
python3 study.py note --title "학습 주제" --source "정리파일경로" --date YYYY-MM-DD
```

- 메모가 대화에 있으면 `--source`를 생략하고 생성된 README에 정리하세요. 기존 파일을 가져오면 `notes.md`로 복사되고 README에서 연결됩니다.
- 생성 명령이 출력한 `note-01` 등의 실제 폴더명을 사용하세요. 다음 기록의 번호를 임의로 가정하지 마세요.
- README의 `## 학습 주제 / 목표`, `## 정리 내용`, `## 배운 점 / 확인한 내용` 제목을 유지하세요. 제공된 메모를 바탕으로 내용을 구성하고, 출처나 개인 경험을 만들어내지 마세요.
- 제목만 있고 학습 자료가 없으면 템플릿을 만든 뒤 실제 공부한 메모를 요청하세요. 에이전트가 보충한 설명은 사용자의 학습 경험과 구분해 표시하세요.
- 사용자가 내용을 확인했다고 알렸고 필수 항목이 갖춰졌다면 완료 처리하세요. 사용자가 아직 확인하지 않은 새 설명을 추가했다면 먼저 초안을 보여주세요.

```bash
python3 study.py done note-01 --date YYYY-MM-DD
```

## 기존 기록 보완 (코딩 에이전트)

사용자가 ‘기록 보완’, ‘README와 meta 채워줘’, ‘Wiki용으로 정리’를 요청하면
새 기록을 만들지 말고 요청 범위의 기존 기록을 읽어 보완하세요.
날짜·작성자 범위를 명확히 하고, 전체 저장소나 다른 사람의 기록으로 임의 확대하지 마세요.
자세한 구조와 명령은 [docs/wiki.md](docs/wiki.md)를 참고하세요.

1. 해당 `solution.*`, `README.md`, `meta.json`과 노트의 `notes.md`를 읽으세요.
   코드는 실행 전에 읽고, 사용자 요청 없이 알고리즘을 교체하지 마세요.
2. 기존 문장·회고·리뷰 요청을 보존하며 빈 TODO와 부족한 설명을 보완하세요.
   문제 요약은 제공된 설명 또는 확인한 원문에 근거하고, 풀이·복잡도는 실제 코드에 근거합니다.
   문제의 대표 알고리즘과 이 풀이가 실제로 사용하는 알고리즘을 혼동하지 마세요.
3. 예제는 ‘예상 결과’와 ‘직접 실행 결과’를 구분하세요. 실행하지 않았다면 명시하세요.
   채점 결과, 소요 시간, self/hint/answer, 개인적으로 배웠다는 경험은 추측하지 마세요.
   난이도는 확인한 원문 또는 사용자 제공 값만 사용하고, 불확실한 값은 비워 두세요.
4. 자료구조·알고리즘·태그는 다음 명령으로 **완료 상태를 유지한 채** 보완하세요.
   이 명령은 LLM이 아니라 에이전트가 정한 값을 적용합니다.

```bash
python3 enrich.py records/YYYY/MM/DD/사용자명/programmers-문제번호 \
  --data-structures "배열, 해시" --algorithms "문자열 처리" --tags "문자열"
python3 enrich.py records/YYYY/MM/DD/사용자명/note-01 --tags "Git, 브랜치"
```

5. 기본은 빈 필드만 채웁니다. 기존 분류가 잘못됐다는 근거가 있을 때만 명시한 필드에
   `--replace`를 사용하세요. 별칭은 `study_wiki/taxonomy.json`에서 정규화합니다.
   문제의 README `## 풀이 정보`도 동기화됩니다. 임의로 meta 상태나 일일 목록을 수정하지 마세요.
6. `## Wiki 요약`을 추가할 수 있습니다. 실제 접근·핵심 제약·연결할 개념을 2~4문장으로 쓰고,
   새 설명은 ‘코딩 에이전트 보완’으로 구분하세요. 없는 학습 경험을 추가하지 마세요.
   노트의 필수 제목 세 개를 유지하고, 새로 보충한 설명은 사용자 검토 전 초안으로 취급합니다.
7. 채택한 리뷰는 선택 항목 `## 리뷰로 확인한 내용`에 내용·확인 방법·PR 또는 댓글 링크를 남기세요.
   AI·사람의 제안을 모두 정답으로 간주하지 말고, 미확인 제안은 검토 중임을 표시하세요.
8. `python3 study.py check`와 `python3 wiki.py build --include-drafts`로 검증·미리보기를 만드세요.
   문제의 정답 확인 또는 노트 내용 확인이 기존 대화에서 이미 제공됐으면 반복 확인 없이
   기존 `done` 절차를 진행하세요. 확인이 없으면 보완 초안까지 진행하고 누락된 확인만 요청하세요.
9. 일일 PR은 당일 본인 기록에 한정합니다. 이전 날짜나 여러 참여자의 분류 보완 요청은
   별도 관리 브랜치·PR로 처리하세요. `.study/wiki/` 결과는 커밋하지 마세요.

`AGENTS.md`는 자동 실행기가 아닙니다. 사람이 요청했을 때 코딩 에이전트가 위 작업을 수행합니다.
GitHub Actions는 PR에서 Wiki 미리보기를 생성하고, main merge 후 검토된 원본으로 공유 Wiki를 배포합니다.

## 검증과 PR 준비

1. 생성은 `new`/`note`, 완료 상태 변경은 `done`, 일일 목록 갱신은 `index` 등 기존 명령으로 처리하세요. `meta.json`의 완료 상태나 자동 생성된 일일 README를 수동으로 맞추지 마세요.
2. `python3 study.py check`로 형식을 검사하세요. 초안도 이 검사를 통과할 수 있으므로 검사 성공을 완료·정답 판정으로 설명하지 마세요.
3. 그날의 모든 기록이 완료되었고 사용자가 제출 준비를 요청했다면 `python3 study.py prepare --date YYYY-MM-DD`를 실행하세요. draft가 남아 있으면 어떤 기록인지 알려주세요. 목표 미달은 제출을 막지 않으며, 자율 참여도 같은 제출 절차를 사용합니다.
4. `.study/PR.md`는 `prepare`를 다시 실행하면 덮어써집니다. 기존에 사용자가 작성한 리뷰 요청이 있다면 보존해 다시 반영하세요.
5. 일일 변경 범위는 `records/YYYY/MM/DD/사용자명/`과 필요한 `members/사용자명.json`입니다. 목표를 과거부터 바꾸면 다른 날짜의 일일 목록도 갱신될 수 있으므로, 이런 변경은 별도 관리 PR로 분리하세요.
6. commit·push·PR 생성은 사용자가 요청한 범위에서 수행하세요. 단순 기록 정리 요청이면 파일과 검증 결과, 다음 실행 명령을 전달하세요. 이미 업로드까지 요청했다면 반복 승인을 요구하지 말고 변경 파일을 확인한 뒤 진행하세요. 일괄 `git add .` 대신 해당 기록과 참여자 파일을 지정하세요.
7. PR은 사용자·날짜별 하나입니다. 기존 PR에 추가할 때는 같은 브랜치에 반영하세요. 다른 참여자의 승인이나 리뷰를 대신 꾸며내지 마세요.

마지막에는 생성·수정한 파일 경로, 확인한 내용, draft/완료 상태, 남은 사용자 확인사항을 짧게 알려주세요. 업로드를 했다면 커밋·브랜치·PR 주소 등 실제 결과를 함께 전달하세요.

## 도구나 공용 문서를 개발하는 요청

위의 일일 브랜치·기록 범위 규칙은 참여 기록 작업에 적용됩니다. 사용자가 시스템 구현이나 공용 문서 수정을 요청하면 실제 요청 범위의 파일을 수정하고 일일 기록 PR과 분리하세요. CLI 변경은 `python3 -m unittest discover -s tests -v`와 `python3 study.py check`로 검증하세요. 문서만 변경할 때는 명령·링크·`git diff --check`를 확인하면 됩니다.
Wiki 변경은 추가로 `node --test tests/wiki_search.test.cjs`, `node --check study_wiki/assets/app.js`, `python3 wiki.py build`를 실행하세요.


## Wiki 개념 노트와 기출

- 개념 연결은 실제 사용한 자료구조·알고리즘·태그에서 생성합니다. 미분류를 임의 사실로 채우지 마세요.
- 기출은 확인된 출처만 `enrich.py --exam-name ... --exam-organization ... --exam-year ... --exam-round ... --exam-url ...`로 기록하세요.
  제목이나 플랫폼만 보고 회사·연도·기출 여부를 추측하지 마세요. `exam` 전체 교체는 `--replace`를 사용합니다.
- 사람이 요청한 개념 학습 정리는 기존 `study.py note` 흐름을 따릅니다.
  자동 Nemotron 초안은 별도 `wiki_notes/generated/`에 저장하며 직접 작성한 note와 구분하고 목표에 합산하지 않습니다.
- AI 노트 생성 요청이 있으면 [docs/wiki.md](docs/wiki.md)의 생성 절차를 사용하세요.
  API 없이 대상을 보려면 `python3 -m study_wiki.generate --topic auto --dry-run`을 사용합니다.
  생성은 무료 Nemotron만 허용하며 모델 장애·한도 초과 시 유료 전환하지 않습니다.
- 생성 결과의 원본 인용과 설명을 검토하세요. 검토 완료를 확인한 경우에만 AI 노트 meta의
  `review_status`를 `reviewed`로 바꾸고, 원본 변경으로 오래된 노트는 수정·재생성 또는 제거를 검토하세요.
  원본 학습 기록의 완료 상태와 AI 노트 검토 상태를 혼동하지 마세요.
- Wiki 노트 생성은 별도 관리 PR입니다. 문제·사람 작성 노트의 일일 PR 범위를 넓히지 마세요.

- AI 개념 노트는 대상 완료 기록 전체를 탐색하고 원본을 분할·통합합니다. 기록 수를 24개 등으로 자르지 마세요.
  `.study/wiki-analysis/`는 진행 중 분석과 재사용 캐시입니다. Git에 올리지 말고 중단 후 같은 요청을 재실행하세요.
  `none`·`null`·미분류 표시는 개념이 아니며 원본에 남아 있어도 Wiki 개념에서 제외합니다.
